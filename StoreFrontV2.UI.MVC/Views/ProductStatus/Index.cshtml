@model IEnumerable<StoreFrontV2.DATA.EF.ProductStatu>

@{
    ViewBag.Title = "Product Status";
}

<h2>@ViewBag.Title</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table data-table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.StatusID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.StatusID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.StatusID })
                </td>
            </tr>
        }
    </tbody>
</table>

@section scripts{

    <script>

        $(document).ready(function () {
            $('.data-table').DataTable({
                responsive: true
            });
        });

    </script>

}